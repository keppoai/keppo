- [x] Re-read `tmp/pr-review-responder/context.json`, `tmp/pr-review-responder/context-summary.md`, and the trusted PR context to self-review the current PR state, changed files, active threads, prior fix attempts, and remaining scope.
- [x] Investigate the failing `CI Result` check and identify which underlying job failures still make it red.
- [x] Investigate the failing `e2e / Local Convex 2/2` check with concrete evidence from logs and determine whether each failing spec is related to this PR's changed files and scope.
- [x] If the `e2e / Local Convex 2/2` failures are unrelated to this PR, document that decision for the eventual `summaryComment` instead of planning speculative code changes.
- [x] Review the active trusted thread about JSLite sidecar environment inheritance in `packages/shared/src/code-mode/sandbox-jslite.ts` and decide the code/doc/test work needed.
- [x] Review the active trusted thread identified by cubic about rejecting `constructor` and `prototype` structured-value keys and decide whether further code/test work is needed.
- [x] Review the active trusted thread on `docs/dev-setup.md` and make the JSLite sidecar build requirements explicit if still needed.
- [x] Review the active trusted thread on `docs/self-hosting-setup.md` and make the JSLite sidecar build requirements explicit if still needed.
- [x] Review trusted review and issue-comment feedback items that do not have thread IDs, including prior Codex/Claude/keppo-bot/cubic summaries, and determine whether any remaining valid issues still need code or docs changes.
- [x] Implement the required code, test, and documentation changes to address all remaining valid trusted feedback and any PR-related CI failures.
- [x] Run focused local verification for the changed JSLite/code-mode area and any other directly affected tests needed to prove the fixes.
- [x] Run required local E2E verification if I fix any PR-related failing E2E specs; otherwise confirm no PR-related E2E rerun is required.
- [x] Run `pnpm check`, fix every remaining failure until it passes, and re-run it to green.
- [ ] Create a local git commit on this feature branch with a Conventional Commit message covering the fixes made in this pass.
- [ ] Re-read `TODOS.md` and confirm every item is marked complete before writing `tmp/pr-review-responder/actions.json`.
- [ ] Write `tmp/pr-review-responder/actions.json` with the required `summaryComment` and `threadActions`.
- [ ] Delete `TODOS.md`.
