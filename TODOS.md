- [x] Self-review `tmp/pr-review-responder/context.json` and `tmp/pr-review-responder/context-summary.md`, plus `docs/rules/product-principles.md`, to confirm the authoritative PR state and valid feedback scope
- [x] Rebase the current branch onto `upstream/main` and resolve every merge conflict
- [x] Run `pnpm check` immediately after the rebase and fix any breakage introduced by the rebase before moving on
- [x] Investigate the non-E2E `fix-pr` workflow run from the PR context and confirm whether it exposes any actionable failure beyond the mandatory local tasks for this pass
- [x] Review the active trusted thread on `convex/ai_credits.ts:620` and decide whether to implement a fix or leave it open with an explicit explanation
- [x] Review the active trusted thread on `convex/ai_credits.ts:399` and decide whether to implement a fix or leave it open with an explicit explanation
- [x] Review the active trusted thread on `cloud/api/billing.ts:248` and decide whether to implement a fix or leave it open with an explicit explanation
- [x] Review the active trusted thread on `convex/ai_credits.ts:539` and decide whether to implement a fix or leave it open with an explicit explanation
- [x] Review the active trusted thread on `convex/ai_credits.ts:775` and implement or verify the fractional-credit depletion/exhaustion fix
- [x] Review the active trusted thread on `convex/ai_credits.ts:851` and implement or verify the gateway `max_budget` update after purchased credits change
- [x] Review the active trusted thread on `convex/ai_credits.ts:537` and decide whether to implement a fix or leave it open with an explicit explanation
- [x] Review the active trusted thread on `apps/web/app/lib/server/automation-api.ts:951` and implement or verify the post-failure credit-sync error-handling fix
- [x] Review the active trusted thread on `apps/web/app/lib/server/automation-runtime.ts:1108` and decide whether to implement a fix or leave it open with an explicit explanation
- [x] Review the active trusted thread on `apps/web/src/lib/automations-view-model.ts:855` and implement or verify the trailing-zero formatting fix
- [x] Review the active trusted thread on `apps/web/src/components/automations/automation-prompt-box.tsx:751` and decide whether to implement or leave it open with an explicit explanation
- [x] Review the active trusted thread on `apps/web/app/lib/server/automation-api.ts:908` and decide whether to implement a fix or leave it open with an explicit explanation
- [x] Review trusted review bodies and trusted issue comments without thread IDs, and address any still-valid feedback through code changes or the final summary comment
- [x] Investigate whether any E2E failure in the PR context is still active and PR-related; if not, document why no local E2E rerun is required for this pass
- [x] Make all required code, spec, and rule updates for the valid feedback items
- [x] Run targeted verification for any changed tests needed to prove the fixes
- [x] Run `pnpm check` after all changes and keep fixing issues until it passes cleanly
- [ ] Re-read `TODOS.md` and confirm every item is complete before writing `tmp/pr-review-responder/actions.json`
- [ ] Write `tmp/pr-review-responder/actions.json` with the required `summaryComment` and `threadActions`
- [ ] Delete `TODOS.md`
- [ ] Create a local git commit with a Conventional Commit message for any code changes made in this pass
