- [x] Re-read `tmp/pr-review-responder/context.json`, `tmp/pr-review-responder/context-summary.md`, and `docs/rules/product-principles.md` to confirm the full PR state before making changes
- [x] Rebase the current branch onto `upstream/main`, resolve every merge conflict carefully, and complete the rebase
- [x] Run `pnpm check` after the rebase to establish the current local baseline and catch any breakage introduced by conflict resolution
- [x] Investigate the failing `e2e / Local Convex 1/2` GitHub Actions check with concrete evidence from read-only CI logs and identify the root cause
- [x] Investigate the failing `CI Result` check and determine whether it is only an aggregate failure or exposes an additional root cause
- [x] Address the Codex trusted issue comment about enforcing a bounded `execute_code.description` contract on the backend
- [x] Address trusted review thread `PRRT_kwDORysWq854pvnS` in `apps/web/src/components/automations/run-chat-bubble.tsx` about regex backtracking risk and a code-size guard
- [x] Address trusted review thread `PRRT_kwDORysWq854pvnU` in `apps/web/src/components/automations/run-chat-bubble.tsx` about avoiding unnecessary re-tokenization on rerender
- [x] Address trusted review thread `PRRT_kwDORysWq854pvnW` in `apps/web/src/components/automations/run-chat-bubble.tsx` about constraining expanded code-block height
- [x] Address trusted review thread `PRRT_kwDORysWq854pvnY` in `apps/web/src/components/automations/run-chat-bubble.tsx` about adding a disambiguating accessibility label
- [x] Make any additional code, test, or spec updates required by the CI diagnosis and trusted feedback
- [x] Run the required local E2E verification for any failing E2E spec fixes, or confirm with evidence that no local E2E rerun is required because no failing spec remains to reproduce
- [ ] Run `pnpm check` again and keep fixing issues until it passes cleanly
- [ ] Self-review the final diff against the PR context, confirm every TODO is complete, and decide the correct thread actions
- [ ] Write `tmp/pr-review-responder/actions.json` with the required summary and thread actions only after every other checklist item is done
- [ ] Delete `TODOS.md` after `tmp/pr-review-responder/actions.json` has been written successfully
