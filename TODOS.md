- [x] Re-read PR context in `tmp/pr-review-responder/context.json` and `tmp/pr-review-responder/context-summary.md` and self-review the current PR state before making changes
- [x] Rebase the current branch onto `upstream/main` and resolve every merge conflict
- [x] Inspect the failing CI checks in the PR context and identify each concrete failure to investigate
- [x] Use read-only `gh` commands to fetch the failing workflow logs for `CI Result` / `e2e / Test 1/4` and determine the root cause with evidence
- [x] Address the failing E2E automation dispatch `401 invalid_secret` problem in code or test harness
- [x] Review trusted PR feedback items, including `trustedReviews` and `trustedIssueComments`, and confirm whether any code or reply actions are needed
- [x] Make any required code and spec/rule updates implied by the fix
- [x] Run required local E2E verification for the failing spec(s) if code changes are made for the E2E failure
- [x] Run `pnpm check` and fix every issue until it passes
- [ ] Create a local git commit with a Conventional Commit message for the completed fix
- [ ] Re-read `TODOS.md` and verify every item is marked complete before writing `tmp/pr-review-responder/actions.json`
- [ ] Write `tmp/pr-review-responder/actions.json` with the required summary and thread actions
- [ ] Delete `TODOS.md`
