- [x] Self-review the PR context, changed file scope, and product-principles guidance before making decisions.
- [x] Investigate the `fix-pr` check state and confirm whether it needs any action or is only this workflow run in progress.
- [x] Investigate the failing `CI Result` check and identify the concrete underlying failing job(s).
- [x] Investigate the failing `e2e / Local Convex 2/2` check, compare the failing spec(s) against `changedFiles`, and decide whether the failure is PR-related or unrelated.
- [x] Document the `e2e / Local Convex 2/2` decision in the final `summaryComment` instead of making a speculative code fix if it is unrelated to this PR.
- [x] Re-review the earlier Codex feedback item about cyclic array values crashing the JSLite bridge and confirm the current branch already addresses it.
- [x] Re-review the earlier Codex feedback item about cargo fallback leaving timed-out sidecars running and confirm the current branch already addresses it.
- [x] Re-review the earlier Claude/cubic feedback item about over-broad `error.code` => `sandboxUnavailable` classification and confirm the current branch already addresses it.
- [x] Re-review the earlier Claude/keppo-bot feedback item about the strict-mode JSLite env error wording/aggregation and fix any remaining issue.
- [x] Re-review the earlier cubic feedback item in `docs/dev-setup.md` about `KEPPO_JSLITE_PROJECT_PATH` support and prebuilt sidecar/Rust wording, and fix any remaining issue.
- [x] Address thread `PRRT_kwDORysWq856U3CY` in `packages/shared/src/code-mode/sdk-generator.ts` about JSLite SDK namespace generation breaking reserved keywords.
- [x] Address thread `PRRT_kwDORysWq856U3Rz` in `packages/shared/src/code-mode/sandbox-jslite.ts` about flushing the final stdout fragment on EOF.
- [x] Address thread `PRRT_kwDORysWq856U4Rr` in `packages/shared/src/code-mode/sandbox-jslite.ts` about missing structured `failure` data for non-validation sidecar errors.
- [x] Address thread `PRRT_kwDORysWq856U4R8` in `packages/shared/src/code-mode/sandbox-jslite.ts` about abandoned `once(stream, "drain")` promises causing unhandled rejections.
- [x] Address thread `PRRT_kwDORysWq856U5ID` in `apps/web/app/lib/server/api-runtime/env-schema.ts` about aggregating strict-mode JSLite env failures with other missing env errors.
- [x] Address thread `PRRT_kwDORysWq856U5IF` in `packages/shared/src/code-mode/sandbox-jslite.ts` about prototype pollution in structured object decoding/encoding.
- [x] Add or update tests covering every behavior change required by the thread fixes.
- [x] Update any docs/specs/rules that need to change to stay in sync with the code changes made in this pass.
- [x] Run the relevant targeted local verification for any PR-related fix beyond `pnpm check` (unit tests and, if applicable, required E2E verification).
- [x] Run `pnpm check` and keep fixing issues until it passes.
- [ ] Review the final diff and create a local Conventional Commit for this pass.
- [ ] Re-read `TODOS.md` and confirm every item is marked done before writing `tmp/pr-review-responder/actions.json`.
- [ ] Write `tmp/pr-review-responder/actions.json` with the required `summaryComment` and `threadActions`.
- [ ] Delete `TODOS.md`.
