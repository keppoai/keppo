- [x] Read `tmp/pr-review-responder/context.json` and `tmp/pr-review-responder/context-summary.md`
- [x] Consult `docs/rules/product-principles.md`
- [x] Self-review the PR context and current branch state before changes
- [x] Rebase the current branch onto `upstream/main` and resolve any merge conflicts
- [x] Investigate the failing `e2e / Checks` GitHub Actions job and gather concrete root-cause evidence from read-only logs
- [x] Review the active trusted thread on `convex/automation_runs.ts:1210` about operator-visible claim failure diagnostics and decide whether code changes are needed
- [x] Review the active trusted thread on `cloud/convex/automation_scheduler.ts:200` about dispatch-token domain separation and decide whether code changes are needed
- [x] Review the active trusted thread on `convex/automation_runs.ts:1249` about consuming the token before downstream lookups succeed and decide whether code changes are needed
- [x] Review the active trusted thread on `convex/automation_runs.ts:1193` about OCC contention in `claimAutomationRunDispatchContext` and decide whether code changes are needed
- [x] Review trusted top-level feedback in `trustedReviews` and `trustedIssueComments` and ensure all valid feedback is addressed in code or explicitly explained
- [x] Implement all required code and documentation changes, including any generated-file updates
- [x] Run targeted E2E verification if the CI context proves an E2E product/test failure that was fixed in code
- [x] Run `pnpm check` and fix every failure until it passes
- [x] Self-review the final diff and PR context to decide thread resolutions/replies and summary content
- [ ] Re-read `TODOS.md` and confirm every item is complete before writing `tmp/pr-review-responder/actions.json`
- [ ] Write `tmp/pr-review-responder/actions.json`
- [ ] Delete `TODOS.md`
