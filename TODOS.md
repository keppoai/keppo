- [x] Re-read PR context in `tmp/pr-review-responder/context.json` and `tmp/pr-review-responder/context-summary.md` to self-review the current PR state before making changes.
- [x] Investigate the non-success `fix-pr` GitHub Actions check and confirm whether there are actionable CI failures beyond the currently open review feedback.
- [x] Validate the `agentLabel` runtime bug in `scripts/security-review-recent/publish-advisories.mjs` and fix it if valid.
- [x] Validate the unpinned `@anthropic-ai/claude-code` install in `scripts/security-review-recent/run-claude.sh` and fix it if valid.
- [x] Validate the `--dangerously-skip-permissions` usage in `scripts/security-review-recent/run-claude.sh` against repo security rules and fix it if valid.
- [x] Validate the Claude session log handling in `scripts/security-review-recent/run-claude.sh` and workflow steps, then preserve/upload logs if needed.
- [x] Review `trustedReviews` and `trustedIssueComments`, ensure each valid feedback item is addressed in code or explicitly accounted for in the final PR summary.
- [x] Update any related docs/specs/rules if the fixes reveal a reusable rule or behavior change that must stay in sync.
- [x] Run `pnpm check` and fix every failure until it passes.
- [x] Run required local E2E verification if the CI context includes failing E2E tests and the fixes touch that area.
- [x] Self-review the final diff and confirm each trusted thread/action has the correct resolution state and reply requirement.
- [ ] Create a local git commit with a Conventional Commit message for the completed fixes.
- [ ] Re-read `TODOS.md` and confirm every item is marked done before writing `tmp/pr-review-responder/actions.json`.
- [ ] Write `tmp/pr-review-responder/actions.json` with the required summary and thread actions.
- [ ] Delete `TODOS.md`.
