{"title":"fix(automations): remove redundant runtime details from the builder","summary":"Hide resolved runtime and runtime mode details from the automation generation settings step without changing saved behavior.","rationale":"The automation builder still preserves the same model and network settings, but it no longer shows the redundant resolved runtime and runtime mode copy that cluttered the review flow. The accompanying builder spec now verifies those fields stay hidden on the settings step. Closes #68."}
