interface:
  display_name: "Security Review Recent"
  short_description: "Review recent commits for severe security bugs."
  default_prompt: "Use $security-review:recent to review the last 7 days of recent commits for real critical and high severity security vulnerabilities, verify them, and emit JSON findings."
