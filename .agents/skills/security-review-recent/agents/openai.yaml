interface:
  display_name: "Security Review Recent"
  short_description: "Review recent churn and broad code areas for severe security bugs."
  default_prompt: "Use $security-review:recent to review recent code with a 2-3 / 2-3 / 2-3 split across past-day, past-week, and broad-coverage review buckets for real critical and high severity security vulnerabilities, then verify and emit JSON findings."
